---
title: Resource Page Template
description: UUse this page for things like embedded tools like forms and login portals
i18nReady: true
type: base
---
import Button from '~/components/Button.astro'
import ContributorList from '~/components/ContributorList.astro'
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro'
import PlaceholderContent from '~/components/PlaceholderContent.astro'

Use iframes and our pre-made resource components.  Any service that gets commonly used can be set up with a premade component where we can plug in any config options.  

<PlaceholderContent/>

