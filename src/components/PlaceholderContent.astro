---
// This is the frontmatter section where you can import other components or scripts if needed
import Button from '../components/Button.astro'
---

<div class='PlaceholderContent'>
  <div class='title'>
    [Placeholder Content]
  </div>
</div>
  
  <style>
    :root {
    --blue: #007B98;
    --teal: #00DFC3;
    }
   .PlaceholderContent{
    width: 100%;
    height: 25ch;
    max-width:100%;
    max-height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    background-color: var(--theme-bg-gradient-top);
    background-image: repeating-linear-gradient(45deg, var(--theme-bg-gradient-top), var(--theme-bg-gradient-top) 35px, var(--theme-bg-gradient-bottom) 35px, var(--theme-bg-gradient-bottom) 70px);
    color: var(--color-gray-95);
    font-family: var(--font-mono);
    font-size: var(--theme-text-lg);
    padding: 2em;
   }
  </style>
  